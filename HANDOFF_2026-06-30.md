# 🛒 交換マーケット Bot 引き継ぎ資料（2026-06-30）

新しいセッション（Fable等）はまずこれを読めば全体を把握できる。コードは全部 GitHub `me11112222/brainrot-market` にある。

---

## 🆕 2026-07-25 🎁抽選（プレゼント企画）システム新設（push済→VM反映で有効化）

4万人達成記念の激レア配布用。コードは `src/giveaway.js`（独立モジュール・customIdは `gw_` 名前空間・30秒の締切監視ループ）。**スレッドを一切使わない**（カード1枚＋DB行）＝1000枠を消費しない。

- **`/抽選作成`**（ManageGuild）→ 任意オプション `最低アカウント日数`（既定0＝誰でも）→ モーダル5項目（タイトル／賞品／当選人数1-20／締切時間1-336h／メモ）。実行したチャンネルにカードを投稿。
- **賞品は図鑑と自動照合**：`catalog.searchNames`（全語AND）→ ダメなら語ごと → それでもダメなら `suggestNames`（あいまい）の3段構え。**カタカナ・全角も通る**（例「ディアディア」「ＳＵＮＮＹ」）。「ネオン/天使/Devil」等の語を書くと**変異スキンの画像**に自動で切り替わる（そのキャラが持っていない変異ならDefaultへフォールバック）。一致しなければ画像なしでテキストのみ。
- **カード**: 賞品画像デカ表示＋⚔️戦闘力(★5換算)/💎レア度、`<t:R>`の**締切カウントダウン**、**参加者数ライブ更新**（4秒デバウンスで編集レート制限を回避）、🎟️参加ボタン1つ。
- **参加**: ワンタップ＋エフェメラル返信。`giveaway_entries` の複合PKで**1人1票**（重複はDB側で弾く）。`gwjoin` レート制限5/分。`min_acct_days>0` の時は `user.createdTimestamp` で判定（**GuildMembersインテント不要**）。
- **抽選**: 締切到達で自動。`crypto.randomInt` による部分Fisher-Yates＝暗号学的に公平（Math.random不使用）。参加者・当選者はDBに残るので**後から検証可能**。🥁「抽選中…」を出して3秒後に結果へ編集（発表演出）。
- **発表**（ユーザー指定の形）: **当選者を@メンション＋`ユーザータグ`＋`ID: 数値ID`で全体表示**（れじぇくんが後から呼び出せるように）。参加人数・当選確率も表示。ホストにもピン（`📮 <@host> 受け渡しよろしく`）。**受け渡し部屋は作らない**（運営が手動で声かけする運用と決定）。
- **`/抽選終了 <番号>`** = 締切を待たず**今すぐ締め切って抽選＆発表**（`ends_at`を現在時刻に更新してから即drawGiveaway）。**`/抽選中止 <番号>`** = 抽選せず中止（当選者なし・カードは🛑表示）。
- **二重抽選ガード**: `drawing` Set ＋ 実行直前に `getGiveaway` でstatusを再確認するので、締切ループと `/抽選終了` が同時に走っても1回しか引かない。
- 停止中に締切が過ぎても**起動直後の1回目のtickで拾って抽選する**（取りこぼしなし）。エラー時は `drawn` にして無限リトライを防ぐ。
- DB: `giveaways` / `giveaway_entries` / `giveaway_winners`（自動マイグレーション）。ローカルでDB層＋照合ロジックのスモークテスト済み。
- ⚠️ 運用メモ: **🛒マーケットのパネルチャンネル以外**で `/抽選作成` するのが無難（パネルのスティッキー再投稿とカードが上下する）。お知らせ系チャンネル推奨。

---

## 🆕 2026-07-22 出品者も退出可 ＋ 図鑑パネル自動再投稿（push済→VM反映で有効化）

- **出品者の🚪退出を解禁**（要望「相手が抜けたのに出品者が抜けれない」）。出品者が押すと**部屋を解散**（`db.deleteRoom`＋5秒後にスレッド削除・部屋には `SELLER_LEFT_NOTICE`）。**出品はactiveのままフィードに残る**（他の買い手と取引可）／**自分で抜けたので不在ストライクは付かない**（自動再出品の対象外）。i18n: `seller_left_room`（`seller_cant_leave` は未使用のまま残置）。
- **INDEX Bot(Python)**: `on_ready` で `repost_panel_on_start()` を実行＝**図鑑を更新して再起動したタイミングで図鑑パネルを最下部へ貼り直す**（「使ってると上にいく」対策）。履歴50件から自分の旧パネル（embed title `🔍 Fight Index`）を削除して重複防止、再接続の多重実行は `_panel_reposted` でガード。`sticky.json` に `char_names` スナップショットを保存し、**増えた分だけ**「📚図鑑を更新したよ／🆕新キャラ名・合計N体」を告知（初回はスナップショット未保存なので告知なし＝全253体列挙の事故防止）。図鑑パネルのチャンネル: `1514317762892267731`。

---

## 🆕 2026-07-22 VM窒息インシデント（メモリ枯渇）

7/21夜間〜7/22、**マーケットBotがオフライン**（日報停止・メンバーリストから消失）。原因＝**RAM 958MBスワップ無し**の環境で、夜間の unattended-upgrade が完走できず3世代積み重なってハング（Dステート）→node(マーケット)が犠牲死→systemdのD-Busすら`Connection timed out`。ps実測: INDEX Bot(python) 266MB / unattended-upgrade群 約380MB / GCP ops-agent 61MB。
**復旧＝`sudo reboot` 一択**（Dステートはkill不可）。再発防止＝スワップ1GB＋Botごとの`MemoryMax`（drop-in）＋ops-agent停止。手順の完全版は **DEPLOY.md §11**。`deploy/brainrot-market.service` にも MemoryHigh/Max を追記済み。

---

## 🆕 2026-07-05 パーティ募集システム（⚔️ボス戦/🔮儀式）新設

旧・ボス戦募集フォーラム（スレッド乱立）の置き換え。マーケットと同じ「DB行＋カード＋プライベートスレッド1本」方式。コードは `src/party.js`（マーケットから独立・customIdは `pty_` 名前空間・独自の60秒メンテループ）。

### 使い方（運営）
- 専用チャンネルで **`/募集パネル設置`**（⚠️ マーケットのパネル/フィードと同じチャンネルは拒否される＝スティッキー衝突防止）。募集カードも部屋もそのチャンネルにできる。パネルはスティッキー＋60秒ごと最下部維持。

### 仕様
- **⚔️ボス戦**（毎週水曜ボスラッシュ・8人固定）: ボタン→モーダル（FN ID必須・戦闘力必須1000〜24000想定・参加条件⚔️任意・メモ）→ 募集カード＋部屋が即出来る。
- **🔮儀式**: 図鑑の `how_to_get` に ritu を含むキャラ（現在11体・自動追従）から選択→モーダル（FN ID必須・人数2〜8デフォ4・戦闘力任意・メモ）。カードにキャラ画像＋絵文字。
- **参加**: カードの🙋→モーダル（FN ID必須＋戦闘力）→ 参加条件⚔️チェック→部屋に自動追加＋ホストにピン。カードの人数/メンバー一覧が即更新。**満員で全員ピン**＋カード🈵表示＆ボタン無効化。
- **FN IDは部屋の中にだけ表示**（公開カードには出さない）。ホストFNはピン留め、参加者FNは参加アナウンスに載る。
- 部屋: ✅マッチ完了（ホストのみ・カード削除＋部屋解散）／🚪退出（枠が空くとカード再オープン）。操作ボタンはスティッキー。
- **スレッド枠対策（2026-07-06・「1000枠をまた埋めるのでは」対応）**: ①部屋は**最初の参加者が来た時に作る**（`ensurePartyThread`）＝参加者ゼロの募集はスレッド0本 ②部屋は**無人1時間で自動クローズ**（`PARTY_IDLE_TTL`・`parties.last_active`、部屋の発言でリセット）＝出発済みの部屋が居座らない。部屋が作れない時（160006等）は参加自体を中止してデータを汚さない（`pty_room_fail`）。
- 制限: 1人1募集/種別・作成2/分・参加5/分・NGワード/URL弾き・**募集は6時間で自動失効**（TTL、`PARTY_TTL`。カードはスレッドを食わないので長めでOK）。
- DB: `parties` / `party_members`（自動マイグレーション）。設定キー: `party_panel_channel_id` / `party_panel_message_id`。
- **UX強化（2026-07-06・ユーザーの「全角で弾かれた/使いづらい」フィードバック対応）**: 戦闘力パースが寛容に（全角`１４０００`・`1.4万`・`14k`・カンマ・絵文字混じり全部OK、NFKC正規化）。全入力欄にプレースホルダー（薄字の記入例）。**`player_profiles` テーブルでFN IDと戦闘力を記憶→2回目からモーダルに自動入力済み**で出る（作成/参加のたび更新）。
- **発見性強化（2026-07-06・「埋もれる/戦力で探したい」対応）**: パネルに **🔍探す** ボタン＝**自分の記憶済み戦闘力に近い順**（→埋まりかけ優先→新しい順）で募集を最大10件表示、**条件未達（min_power超え）の募集は非表示**、プルダウンからそのまま参加モーダルへ。📋マイ募集に **⬆️上げ直し**（カードを最下部に再掲載・rateOkで10分1回）。パネルに「いま募集中 ⚔️N件・🔮M件」ライブ表示（貼り直しのたび更新）。
- 本番設置済み: チャンネル **🎮￤パーティ募集**（id `1523385541566009475`・日本語カテゴリ）にパネル設置済み（2026-07-06・ボス戦フルサイクル動作確認済み）。

---

## 🆕 2026-07-05 取引フロー強化（push待ち→VM反映で有効化）

### 逃げ出品対策：5ストライク自動取り下げ
- `listings.relist_count` 追加（自動マイグレーション）。**自動**再出品のたびに+1（=出品者の不在回数）、**手動**🔁再出品は0にリセット（生存確認扱い）。
- 不在5回目（`RELIST_STRIKE_MAX=5`）は再出品せず **expired で自動取り下げ**＋フィード告知＋DM（🔁復活ボタン付き）。
- 再出品通知に「不在N回目・5回で自動取り下げ」を明記。

### 両者✅の徹底（実績カウント漏れ対策）
- 片方だけ✅の間は**ルームを無反応クローズしない**。押してない側へ **毎分ピン**（`match_rooms.done_remind_at`）。確認待ちは**最初の✅（`match_rooms.done_at`）から30分まで**＝過ぎたら通常クローズに戻す（リマインドには締切の`<t:R>`カウントダウン付き）。
- リマインド対象は room_members ベース（🚪退出済みはピンしない＝メンション再追加で引き戻さない）。
- **出品者✅済みのまま**閉じた場合＝取引は済んだ扱いで**再出品せずクローズ**（実績カウントなし）。買い手だけ✅→出品者不在はストライク対象。
- 🎉成立メッセージに両者の実績回数＋「N回で実績ロール」進捗を表示。

### 取引ルームの文字削減（子供が読めるように）
- TRADE_TIPS メッセージ廃止。SCAM_NOTICE を4行に圧縮（自己責任/詐欺/両者✅/自動クローズ）。操作説明は `CONTROL_HINT` 1行に統一。ボタンラベル「Done (seller)」→「Done」。パネルの「24hで自動クローズ」表記を「取引成立は2人とも✅」に修正。

### 検索の大幅強化（同日追加）
- **原因判明**: 「SUNNYで出ないのにSUNで出る」＝**全角英字**（ＳＵＮＮＹ）が原因。旧検索は素のtoLowerCase+includesだけだった。
- `src/textnorm.js` 新設: NFKC（全角→半角・半角ｶﾅ→全角）＋小文字化＋スペース/記号除去＋ひらがな→カタカナ＋長音「ー」除去。
- `src/aliases.json` 新設: **全244体のカタカナ読みエイリアス**（例: サニー→SunnyAndMoony、ゴジラ→Gorgonzilla、らしーくれっと→LaSecretCombinasion）。名前→読み(文字列 or 配列)。**新キャラ追加時はここに読みを足す**（add-characterスキル手順6）。/図鑑リロードで再読込される。
- catalog.searchNames/suggestNames を正規化インデックス方式に刷新（スペース区切りは全語含むAND扱い＝"secret la"もOK）。あいまいDiceも正規化キーで実施。
- db.searchByWant（💱逆引き）も正規化部分一致に（LIKE→JSフィルタ、直近3000件スキャン）。

### 図鑑：AbyssalRabiknight + SunnyAndMoony 追加（6/30から止まってた分）
- ステージング（スプレッドシートCSV未マージ＋images/0630サブフォルダ）を整備してパイプライン実行。characters_edit.csv 末尾改行欠落の罠も実際に踏んで修正済み。
- 246体に（Unknown 7体目 / Secret 105体目）。SunnyAndMoonyは11番スキンMatrix持ち（build側でSKIN_SKIP済み・CDNには上がる）。
- 反映には**3リポのpush**＋VM作業（brainrot-images→brainrot-bot の順でpush→VMでpull＋絵文字追加＋/図鑑リロード）。

---

## 🆕 2026-07-02 大型アップデート（push待ち→VM反映で有効化）

### 安定化
- **DBバックアップ一式追加**: `scripts/backup-db.js`（VACUUM INTO・稼働中OK）＋ `deploy/brainrot-backup.{service,timer}`（日次04:30 JST・曜日名7世代ローテ）。**VMでtimer有効化が必要**（DEPLOY.md §10）。
- 定期メンテループに**多重実行ガード**（処理が60秒超えても重複実行しない＝DM二重送信/二重再出品の防止）。
- `unhandledRejection`/`uncaughtException` ハンドラ追加（後者はexit→systemdが再起動）。
- 出品カード送信失敗時は出品をロールバック（孤児出品防止）。

### 機能追加
- **両者✅方式**: 取引完了は出品者＋相手の両方の✅で成立 → `trades` テーブルに実績記録（水増し・一方的クローズ防止）。
- **取引実績**: 出品カードのフッターに「✅取引実績N回」。`/実績ロール設定 <ロール> <回数>` で達成時に自動ロール付与。
- **💞自動マッチング**: 新規出品時に「相手のwantに自分のgive」「自分のwantに相手のgive」を突合、相互マッチ優先で両者にフィードで提案（同一ペア6hデデュープ）。
- **📌ウォッチ**: 検索空振り時に「出品されたら通知」ボタン → 出品されたらフィードでメンション（1人5件・通知は1回きり・14日で自動解除）。
- **📰日報**: `/日報設置` したチャンネルに毎日、**出品総数**＋今日の新規出品・成立取引数＋供給/需要TOP5を自動投稿。（当初は週報だったが2026-07-02にユーザー要望で日次化。設定キーは news_channel_id / last_daily_news）
- **🔥需要バッジ**: 直近7日の需要が3以上かつ供給の2倍以上のアイテムはカードに「🔥人気/HOT」。
- **`/図鑑リロード`**: 再起動なしで characters.json を再読込（新キャラ反映。絵文字は別途アップロード）。

### 改善
- 検索精度: 「探す」はまず `give_name` 完全一致→表記のLIKEで補完。LIKEの `%_\` はエスケープ（ワイルドカード注入防止）。
- 需要ランキング（📊とニュース）は**直近7日窓**の集計に変更（鮮度重視）。
- 掃除の追加: want_hits 30日・matches 7日・watches 14日で自動プルーニング。

### VM反映手順（いつもの＋α）
```bash
cd ~/brainrot-market && git pull && sudo systemctl restart brainrot-market
# バックアップtimer（初回のみ）: DEPLOY.md §10 参照
```
反映後にDiscord側でやること（任意）: `/日報設置`（ニュースを流したいチャンネルで）、`/実績ロール設定`（実績ロールを作ってから）。

### DBスキーマ追加（自動マイグレーション・起動時に適用）
`trades`（取引実績）、`watches`（ウォッチ）、`match_rooms.done_seller`/`done_buyer_id`（両者✅）。

---

## 1. これは何
Discordサーバー **ブレインロットファイト【Fight the BRAINROT】**（guild_id `1456356183613898803` / 約32,800人 / Fortnite(UEFN)ゲーム「ブレファ」コミュニティ / 日英）の **アイテム交換マーケット Bot**。
目的＝コミュニティ活性化 ＆ **Discordの「1サーバー1000アクティブスレッド上限」問題の解決**（旧・交換募集/ボス戦募集フォーラムが枠を食い潰していた）。
根本解決＝出品を**スレッドではなくDB行**にした。取引ルームだけスレッド化し、無反応10分で自動クローズ。

**現状：本番稼働中。公開数分で250〜400件の出品＝実需要あり。**

## 2. Bot / インフラ
- Bot: **MEGA MECHSPOT** (#3281)、CLIENT_ID `1520867492925276180`、Administratorではなくスコープ権限で本番鯖に参加。
- スタック: Node.js **v22.23.1**（`node:sqlite` DatabaseSync に22.5+必須）、discord.js v14 (ESM)。DBは `data.sqlite`（**WALモード**）。
- ホスティング: **GCP e2-micro VM**（無料枠）。ユーザー `hidemeron`。Python製INDEX BOTと相乗り。
  - systemd: `brainrot-market`（この bot）／`brainrot-bot`（INDEX/Python図鑑bot）
  - clone: `/home/hidemeron/brainrot-market`
  - `.env`（VMのみ・非コミット）: `DISCORD_TOKEN` `CLIENT_ID=1520867492925276180` `GUILD_ID=1456356183613898803` `CATALOG_PATH=/home/hidemeron/brainrot-bot/characters.json`
- リポ: **github.com/me11112222/brainrot-market**（現在PUBLIC・秘密は非コミット）。ローカル `C:\Users\reje\brainrot-bot`。
- ⚠️ **ローカルの .env のDISCORD_TOKENは無効（リセット済み）**。有効トークンはVMのみ。ローカルからBot APIは叩けない（GitHub pushは可）。

## 3. チャンネル構成（本番）
| チャンネル | ID | 役割 | メンバー権限 |
|---|---|---|---|
| 🛒マーケット | `1521108045529223298` | 操作パネル専用（出品/探す/マイ/ランキング） | 閲覧＋押すのみ・パネルは常に最下部維持 |
| 📋出品リスト | `1521102798479954031` | 出品カードが流れる＋取引ルームもここに作る | 閲覧のみ（送信NG・スレッド内送信OK・履歴OK） |
| ~~交換マーケット(旧)~~ | `1521093022295785652` | **重複・不要** | **削除推奨**（要ユーザー判断） |

サーバーはロックダウン型（@everyoneはデフォ閲覧/送信不可）。公開＝Memberロールに閲覧許可。

## 4. データ / 図鑑（重要）
- カタログ = `characters.json`（244体、Missing除く242体がトレード可）。マーケットは `CATALOG_PATH` 経由で **INDEX BOTのクローンの characters.json** を読む。
- **正本 = `characters_edit.csv`**（INDEX側・手記入）→ `python build_from_csv.py` → `characters.json` 生成。**これを直せばINDEXもマーケットも両方反映**。
- 画像マスター: `C:\AI\projects\IndexPng`（`T_<名前>_1Default.PNG` 等）→ build が jsDelivr用 `brainrot-images` リポに差分コピー。CDN: `https://cdn.jsdelivr.net/gh/me11112222/brainrot-images@main/<file>`
- アイコン = **アプリ絵文字(Application Emojis)**（最大2000・サーバー枠不要・全鯖で使える）。`scripts/upload-app-emojis.js` で登録、`emojis` テーブルに name→id 保存。起動時 `syncEmojisAcrossGuilds` がアプリ絵文字と突合して不整合を掃除。

## 5. 機能一覧（実装済み）
出品（画像ピッカー: カテゴリ→アイテム→変異/★/特性・任意 / ほしいもの・メモは自由文）／探す（あいまい名前検索・戦闘力近似の類似表示・**💱相手のほしいもので探す=逆引き**）／人気ランキング（供給=出品数・需要=探された数、ユニーク集計）／出品カードに `⚔️戦闘力（★5→base×2.8）・💎レア・💰価格・🏭生産・出品者名+アイコン`／取引ルーム（1出品1ルーム・詐欺注意ピン・コツ投稿・🚪退出・✅取引完了・**無反応5分警告(残り時間`<t:R>`表示)→10分クローズ**）／**自動再出品**（未完了なら時間切れ毎に再出品＋出品者を**@メンション通知**※DMは多くが拒否のためチャンネルmention採用）／出品7日で自動失効／日英ローカライズ（interaction.locale）／荒らし対策（連投1/min・同時出品3・ルーム3/min・メンション封じ・URL/NGワード弾き・サーバーロック=他鯖自動退出）／スケール対策（WAL・index・キャッシュ抑制・メモリ掃除）。

## 6. デプロイ / 更新
```bash
# コード更新
cd ~/brainrot-market && git pull && sudo systemctl restart brainrot-market
# 図鑑データ更新（INDEX側pull=マーケットのCATALOG_PATHも更新される）
cd ~/brainrot-bot && git pull && sudo systemctl restart brainrot-bot
# アプリ絵文字（新キャラ分だけ追加。--resetで全取り直し）
cd ~/brainrot-market && node scripts/upload-app-emojis.js
```
※カタログは起動時読み込み（ホットリロード無し）→新キャラ反映にはmarket再起動が必要。

## 7. メンテ運用
- **ステータス変更（強化/弱体）**: `characters_edit.csv` の attack等を直す → `python build_from_csv.py` → `git push`（brainrot-bot repo）→ VMで両方restart。（画像/絵文字変更不要）
- **新キャラ追加**: `new-characters/追加キャラ.csv` 記入＋`new-characters/images/` に画像 → `node scripts/add-characters.js --apply`（IndexPngへコピー＋characters_edit.csvへ追記）→ `python build_from_csv.py` → push（brainrot-bot＋brainrot-images）→ VMでpull＋`upload-app-emojis.js`＋restart。
- 詳細は `new-characters/README.md`。

## 8. 既知の課題 / TODO（優先度順）
1. 🟡 **DBバックアップ**: スクリプト＋systemd timer追加済み（2026-07-02）。**残り＝VMでtimer有効化**（DEPLOY.md §10 の5コマンド）。理想はさらにVM外への週1コピー。
2. 旧「交換マーケット」(1521093022295785652) 削除（要ユーザー判断）。
3. アプリ絵文字242体のアップロードが完了してるか要確認（レート制限でトリクル）。
4. 旧フォーラム（交換募集/trading）のロック＆誘導（アーカイブ済みだが再投稿防止のロックは未）。
5. リポをデプロイキー方式でprivateに戻す（任意）。
6. 本番ピーク負荷は未検証。`journalctl -u brainrot-market -f` で監視推奨。
7. Phase2（レベル/ランク/進捗バー/35k達成/招待ランキング）は未着手。

## 9. 方針メモ（2026-06-30時点の合意）
使用実績で価値は実証済み。今は「機能追加」より **"安定化・観察"フェーズ**推奨：①DB日次バックアップ ②1〜2週間は新機能を止めて実使用を観察し、ユーザーが詰まった所だけ直す ③ロードマップは実需要ドリブンで。

## 10. 参照
- 自動メモリ（このPCで全セッション自動ロード）: `C:\Users\reje\.claude\projects\C--Users-reje\memory\discord-brainrot-server.md`
- デプロイ手順書: `DEPLOY.md`
- INDEX BOT引き継ぎ: `C:\AI\projects\event-tool\discord-bot\HANDOFF_2026-06-28.md`
- 主要コード: `src/index.js`（起動/ルーティング/サーバーロック）, `src/marketplace.js`（本体）, `src/db.js`（node:sqlite）, `src/catalog.js`（図鑑）, `src/i18n.js`（日英）, `src/moderation.js`（荒らし対策）
